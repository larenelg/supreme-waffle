"use strict";

// hardcoded consts (see assumptions)
// math would be injected here
const TIME_STEP = 1;
const DISTANCE_BETWEEN_FLOORS = 6;
const VELOCITY = 2;

module.exports = {
  TIME_STEP, DISTANCE_BETWEEN_FLOORS, VELOCITY
}